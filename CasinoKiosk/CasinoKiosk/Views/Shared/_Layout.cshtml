


<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    
    <!-- Custom fonts for this template-->
    <link href="~/Assets/Admin/vendor/fontawesome-free/css/all.min.css" rel="stylesheet" type="text/css">
    <link rel="icon" href="~/Assets/Images/icon_web.png" type="image/x-icon">
    <link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">

    <link href="~/Assets/Admin/css/sb-admin-2.min.css   " rel="stylesheet">
  
    <!-- Custom styles for this page -->
    <link href="~/Assets/Admin/vendor/datatables/dataTables.bootstrap4.min.css" rel="stylesheet">

    
</head>
<body id="page-top">
    <!-- Page Wrapper -->
   
    <div id="wrapper">
        <!-- Sidebar -->
        @Html.Partial("_MenuBar")
        <!-- End of Sidebar -->
        <!-- Content Wrapper -->
        <div id="content-wrapper" class="d-flex flex-column">
            <!-- Main Content -->
            <div id="content">

                <!-- Topbar -->
                <nav class="navbar navbar-expand navbar-light bg-white topbar mb-4 static-top shadow">
                    <!-- Sidebar Toggle (Topbar) -->
                    <button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
                        <i class="fa fa-bars"></i>
                    </button>
                    <!-- Topbar Search -->
                    @RenderSection("HeaderContent", false)
                    <!-- Topbar Navbar -->
                    <ul class="navbar-nav ml-auto">
                        <!-- Nav Item - Search Dropdown (Visible Only XS) -->
                        <li class="nav-item dropdown no-arrow d-sm-none">
                            <a class="nav-link dropdown-toggle" href="#" id="searchDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="fas fa-search fa-fw"></i>
                            </a>
                            <!-- Dropdown - Messages -->
                            <div class="dropdown-menu dropdown-menu-right p-3 shadow animated--grow-in" aria-labelledby="searchDropdown">
                                <form class="form-inline mr-auto w-100 navbar-search">
                                    <div class="input-group">
                                        <input type="text" class="form-control bg-light border-0 small" placeholder="Search for..." aria-label="Search" aria-describedby="basic-addon2">
                                        <div class="input-group-append">
                                            <button class="btn btn-primary" type="button">
                                                <i class="fas fa-search fa-sm"></i>
                                            </button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </li>
                        <!-- Nav Item - Alerts -->
                        <!-- Nav Item - Messages -->

                        <div class="topbar-divider d-none d-sm-block"></div>
                        <!-- Nav Item - User Information -->
                        <li class="nav-item dropdown no-arrow">
                            <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <span class="mr-2 d-none d-lg-inline" style="color:blue">
                                    @*<h6 class="m-0 font-weight-bold text-primary">@Session["userName"]</h6>*@
                                    <span class="mr-2 d-none d-lg-inline text-primary">@HttpContext.Current.User.Identity.Name</span>
                                </span>
                                <img class="img-profile rounded-circle" src="~/Assets/Admin/img/usericon.png">
                            </a>
                            <!-- Dropdown - User Information -->
                            <div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">

                                <a class="dropdown-item" href="#">
                                    <i class="fas fa-list fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Activity Log
                                </a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="#" data-toggle="modal" data-target="#logoutModal">
                                    <i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
                                    Logout
                                </a>
                            </div>
                        </li>
                    </ul>
                </nav>
                <!-- End of Topbar -->
                <!-- Begin Page Content -->
                <div class="container-fluid">
                    <!-- Page Heading -->

                    @RenderBody()
                </div>
                <!-- /.container-fluid -->
            </div>
            <!-- End of Main Content -->
            <!-- Footer -->
            <footer class="sticky-footer bg-white">
                <div class="container my-auto">
                    <div class="copyright text-center my-auto">
                        <span>Copyright &copy; IT HOTRAM 2020</span>
                    </div>
                </div>
            </footer>
            <!-- End of Footer -->
        </div>
        <!-- End of Content Wrapper -->
    </div>

    <!-- Scroll to Top Button-->
    <a class="scroll-to-top rounded" href="#page-top">
        <i class="fas fa-angle-up"></i>
    </a>
    <!-- Logout Modal-->
    <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
                <div class="modal-footer">
                    <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
                    <a class="btn btn-primary" href="/Account/LogOff">Logout</a>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="mTimeOut" tabindex="-1" role="dialog" aria-labelledby="mTimeOutLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="mCardInfoLabel">Your current session has been end</h5>
                    <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">Select "OK" below to Login again</div>
                <div class="modal-footer">
                    <a class="btn btn-primary" href="/Account/LogOff">OK</a>
                </div>
            </div>
        </div>
    </div>
    <!-- Bootstrap core JavaScript-->

    <!-- Bootstrap core JavaScript-->
    <script src="~/Assets/Admin/vendor/jquery/jquery.min.js"></script>
    <script src="~/Assets/Admin/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    <!-- Core plugin JavaScript-->
    <script src="~/Assets/Admin/vendor/jquery-easing/jquery.easing.min.js"></script>
    <!-- Custom scripts for all pages-->
    <script src="~/Assets/Admin/js/sb-admin-2.min.js"></script>
    <!-- Page level plugins -->
    <script src="~/Assets/Admin/vendor/datatables/jquery.dataTables.min.js"></script>
    <script src="~/Assets/Admin/vendor/datatables/dataTables.bootstrap4.min.js"></script>
    <script src="~/Assets/Admin/js/jquery.table2excel.js"></script>
    
    
    <script src="~/Assets/Admin/js/jquery.unobtrusive-ajax.js"></script>
    <script src="~/Assets/Admin/js/jquery.unobtrusive-ajax.min.js"></script>
    <script src="~/Assets/Admin/js/bootstrap3-typeahead.min.js?n=3"></script>

    @RenderSection("scriptUser", false)
    @RenderSection("scriptPlayer", false)
    @RenderSection("scriptFOPatronUser", false)
    @RenderSection("script8DragonUser", false)
    @RenderSection("scriptCCUser", false)
    @RenderSection("scriptActivity", false) 
    @RenderSection("scriptTicketPromotion", false)
    @RenderSection("scriptAnalyst", false)
    @RenderSection("scriptPromotion", false)
    @RenderSection("scriptMarket", false)

    <script>
        $(document).ready(function () {
            $('#DailyGrid').DataTable(
                {"order": [[ 0, "desc" ]]
        } );
            $('#FridayLogTable').DataTable({
                "order": [[0, "desc"]]
            });
            $('#RedemptionGrid').DataTable({
                "order": [[0, "desc"]]
            });
            $('#WeeklyLogTable').DataTable({
                "order": [[0, "desc"]]
            });
            $('#listLogTable').DataTable({
                "order": [[0, "desc"]]
            });
            $('#itemlistTable').DataTable({
                "order": [[0, "desc"]]
            });
            $('#playerList').DataTable({
                "order": [[0, "desc"]]
            });
            $('#FOPatronGrid').DataTable({
                "order": [[0, "desc"]]
            });
            //$('#proTbl').DataTable({
            //    "order": [[0, "desc"]]
            //});
            $('#enrollmentTbl').DataTable({
                "bDestroy": true,
                "order": [[0, "desc"]],
                "dom": 'lBfrtip',
                "buttons": [
                    'copy', 'csv', 'excel', 'pdf', 'print'
                ]
            });
            $('#KioskUserGrid').DataTable({
                "order": [[0, "desc"]]
            });
            $('#CCGrid').DataTable({
                "order": [[0, "desc"]]
            });
            $('#POS8DragonGrid').DataTable({
                "order": [[0, "desc"]]
            });
            $('#ActivityGrid').DataTable({
                "order": [[0, "desc"]]
            });

            //Add 20230301 Hantt start
            $('#detailMarTbl').DataTable({
                "bDestroy": true,
                "order": [[0, "desc"]]
            });
            //Add 20230301 Hantt end
        });

        $(function () {
            $('div a[href^="' + location.pathname + '"]').parents("li").addClass('active');
            //Add 20230109 Hantt start
			$('div a[href^="' + location.pathname + '"]').parents("div").addClass('show');
            //Add 20230109 Hantt end
            $('div a[href^="' + location.pathname + '"]').addClass('active');           
        });
    
        $(function () {
            $("#btnExcelExportFOPatronGrid").click(function (e) {
                var table = $("#FOPatronGrid");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "FOPatronUserActionLog" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportCCGrid").click(function (e) {
                var table = $("#CCGrid");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "CCUserActionLog" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportPOS8DragonGrid").click(function (e) {
                var table = $("#POS8DragonGrid");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "POS8DragonUserActionLog" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportKioskUserGrid").click(function (e) {
                var table = $("#KioskUserGrid");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "KioskUserActionLog" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportDailyGrid").click(function (e) {
                var table = $("#DailyGrid");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "DailyGrid" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportFridayLogTable").click(function (e) {
                var table = $("#FridayLogTable");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "FridayLog" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportWeeklyLogTable").click(function (e) {
                var table = $("#WeeklyLogTable");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "WeeklyLog" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportlistLogTable").click(function (e) {
                var table = $("#listLogTable");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "listLog" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExport").click(function (e) {
                var table = $("#itemlistTable");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "itemlist" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });


        });
        $(function () {
            $("#btnExcelExportRedemptionGrid").click(function (e) {
                var table = $("#RedemptionGrid");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "Redemption" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportPlayersList").click(function (e) {
                var table = $("#playerList");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "CIFEPlayersList" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });

        });
        $(function () {
            $("#btnExcelExportActivityGrid").click(function (e) {
                var table = $("#ActivityGrid");
                if (table && table.length) {
                    var preserveColors = (table.hasClass('table2excel_with_colors') ? true : false);
                    $(table).table2excel({
                        exclude: ".noExl",
                        name: "Excel Document Name",
                        filename: "ActivityReport" + new Date().toISOString().replace(/[\-\:\.]/g, "") + ".xls",
                        fileext: ".xls",
                        exclude_img: true,
                        exclude_links: true,
                        exclude_inputs: true,
                        preserveColors: preserveColors
                    });
                }
            });
        });
    </script>     

    @*Add 20230301 Hantt start*@
    <script src="https://cdn.datatables.net/buttons/2.3.4/js/dataTables.buttons.min.js"></script>
    <script src="~/Assets/Admin/js/export/buttons.html5.min.js"></script>
    <script src="~/Assets/Admin/js/export/jszip.min.js"></script>
    <script src="~/Assets/Admin/js/export/pdfmake.min.js"></script>
    <script src="~/Assets/Admin/js/export/vfs_fonts.js"></script>

    <link rel="stylesheet" type="text/css" href="~/Assets/Admin/css/buttons.dataTables.min.css" />
    <script>
        $(document).ready(function () {
            $('#marketingListTbl').DataTable({
                "bDestroy": true,
                "order": [[0, "asc"]],
                "dom": 'Bfrtip',
                "buttons": [
                    'copy', 'csv', 'excel', 'pdf', 'print'
                ]
            }); 

            
        });
    </script>
    @*Add 20230301 Hantt end*@
</body>
</html>
